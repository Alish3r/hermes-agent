@AGENTS.override.md

## Claude Code

- This checkout is on branch `local/alisher`; commit local adaptations there, never on `main` (see "Local checkout conventions" above).
- Use `scripts/run_tests.sh`, never `pytest` directly.
- The full upstream guide is `AGENTS.md`; read the section named in the routing table before editing a subsystem.
